<%-- Cierra la estructura abierta en app-top.jspf. --%>
    </main>

    <%-- Regla y aviso legal al pie del marco (121:87 y 121:89). --%>
    <footer class="pfoot">
        <p>®Sistema de Gestión de Fondos y Tarjetas Empresariales de Empresa S.A de C.V</p>
    </footer>
</div>

<%-- Desenlace de la última operación, si lo hubo. Se pinta solo. --%>
<%@ include file="/WEB-INF/jsp/partials/result-modal.jspf" %>

<script>
    /* Mismo guardián que en el admin: con un modal abierto el fondo no se
       mueve. Ver el comentario en admin-bottom.jspf. */
    (function () {
        var root = document.documentElement;

        function anyOpen() {
            var scrims = document.querySelectorAll(".modal-scrim");
            for (var i = 0; i < scrims.length; i++) {
                if (!scrims[i].hidden) return true;
            }
            return false;
        }

        function sync() { root.classList.toggle("is-modal-open", anyOpen()); }

        new MutationObserver(sync).observe(document.body, {
            subtree: true,
            childList: true,
            attributes: true,
            attributeFilter: ["hidden"]
        });

        sync();
    })();
</script>
<%@ include file="/WEB-INF/jsp/partials/password-eye.jspf" %>
</body>
</html>
